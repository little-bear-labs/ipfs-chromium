component("ipfs") {
  sources = [
    "loader.cc",
    "loader_factory.cc",
  ]
  defines = [  ]
  include_dirs = [
    "."
  ]
  deps = [
    "//base",
    "//components/policy/core/common",
    "//components/services/quarantine",
    "//net",
  ]
  defines = [ "IS_IPFS_IMPL" ]
}
